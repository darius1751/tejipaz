import { Metadata } from "next"

export const metadata: Metadata = {
    title: 'Shop - Products'
}
export default function Products() {
    return (
        <div>
            Products
        </div>
    )
}